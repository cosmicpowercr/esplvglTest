<!--
    Music Item Component
    A single song entry in the music playlist.
    - Displays song file name with music icon
    - Highlights when selected (current_song_index matches this item's index)
    - Normal: icon #4A5666, text #8A94A6 (secondary)
    - Selected: icon #2E7DF6, bg #252F3E, 1px blue border
-->
<component>
    <previews>
        <preview width="220" height="48" style_pad_all="4" />
    </previews>

    <api>
        <prop name="song_name" type="string" default="song.mp3" help="The MP3 file name" />
        <prop name="song_index" type="int" default="0" help="Index in the playlist" />
    </api>

    <styles>
        <!-- Normal (unselected) item style -->
        <style name="style_item_normal"
            bg_color="#bg_screen" bg_opa="100%"
            text_color="#text_secondary"
            radius="#unit_sm"
            pad_hor="#unit_lg"
            pad_ver="#unit_sm"
            width="100%"
            height="48"
            border_width="0"
        />

        <!-- Selected (playing) item style -->
        <style name="style_item_selected"
            bg_color="#bg_item_highlight" bg_opa="100%"
            text_color="#accent_primary"
            border_color="#accent_primary"
            border_width="1"
            border_opa="100%"
            radius="#unit_sm"
        />

        <!-- Text style for song name -->
        <style name="style_song_text"
            text_font="font_14"
        />
    </styles>

    <view width="100%" height="48" style_pad_ver="#unit_xs">
        <lv_label
            text="$song_name"
            align="left"
            width="100%"
        >
            <style name="style_item_normal" />
            <style name="style_song_text" />
            <!-- Selected state is applied by C code at runtime.
                 When the user scrolls and stops for 3s, C code sets the selected
                 state on the item under the scroll position and triggers playback. -->
        </lv_label>
    </view>
</component>
